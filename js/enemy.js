class Enemy {
    constructor() {
        this.x = 160;
        this.y = 120;
        this.width = 44;
        this.height = 83;
        this.speed = 8;
      }
}

// can inherit from player??

// draw enemy by window and appears at a certain time
// draw() {
//     image(game.enemyImage, this.x, this.y, this.width, this.height);
//   }